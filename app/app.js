App({
    /**
     * 当小程序初始化完成以后会触发，且只触发一次
     */
    onLaunch () {
    },
    /**
     * 当小程序从前台进入后台时会触发
     */
    onShow () {
    },
    /**
     * 当小程序从前台进入后台时会触发
     */
    onHide () {
    }
});
